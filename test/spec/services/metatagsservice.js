'use strict';

describe('Service: metaTagsService', function () {

  // load the service's module
  beforeEach(module('spaceshiplabsApp'));

  // instantiate service
  var metaTagsService;
  beforeEach(inject(function (_metaTagsService_) {
    metaTagsService = _metaTagsService_;
  }));

  /*it('should do something', function () {
    expect(!!metaTagsService).toBe(true);
  });*/

});
