'use strict';

describe('Service: windowSize', function () {

  // load the service's module
  beforeEach(module('spaceshiplabsApp'));

  // instantiate service
  var windowSize;
  beforeEach(inject(function (_windowSize_) {
    windowSize = _windowSize_;
  }));

  /*it('should do something', function () {
    expect(!!windowSize).toBe(true);
  });*/

});
